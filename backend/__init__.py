"""Llama Profile Manager (LPM) backend package.

The app version lives in exactly one place - `__version__` below - and is
exposed over the API (GET /api/system/info) and shown in Settings. Bump it
here for every release and keep the git tag in step with it (tag = "v" +
this value, e.g. 0.1.0 -> v0.1.0).
"""

__version__ = "0.2.0"
